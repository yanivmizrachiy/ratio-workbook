# יחס ופרופורציה — SOURCE OF TRUTH

> זהו **מקור האמת היחיד והמחייב** של פרויקט חוברת היחס.
> אין מסמך דרישות נוסף. README, קוד, בדיקות ו־workflows הם מימוש בלבד ואינם רשאים להגדיר כלל שסותר מסמך זה.

## 0. כלל־על: איך עובדים בפרויקט

1. כל בקשה חדשה של יניב לגבי פרויקט היחס נרשמת ומסוכמת כאן **לפני** שינוי בקוד.
2. לפני כל שינוי קוראים את המסמך הזה ובודקים שהשינוי תואם לכללים הקיימים.
3. אם בקשה חדשה סותרת כלל קיים — לא מנחשים ולא מוחקים בשקט; מציגים ליניב את הסתירה לפני מימוש.
4. לאחר שינוי מעדכנים כאן רק אם נוספה/שונתה דרישה. פרטי מימוש טכניים שאינם דרישה נשמרים בקוד/commit ולא יוצרים מקור אמת נוסף.
5. בכל עבודה ארוכה מדווחים ליניב אחוז התקדמות אמיתי ואחוז שנותר עד השלמת **כל הדרישות הפעילות**, לא אחוז משוער שנועד להרגיע.
6. אין לפרסם ל־Production ללא אישור מפורש של יניב לאחר שהוא ראה Preview.

## 1. ארכיטקטורה — ריפו יחיד

- הריפו הקנוני של הפרויקט הוא `yanivmizrachiy/ratio-workbook`.
- לאחר השלמת ההגירה, **אסור** שקוד פעיל של חוברת היחס יישאר תלוי ב־`razpages`, ב־`sources/lovable/ratio-workbook` או בריפו אחר.
- `razpages/sources/lovable/ratio-workbook` הוא מקור הגירה זמני בלבד. סוגרים/מפסיקים להשתמש בו רק אחרי שכל קבצי המקור הנדרשים וכל 55 דפי התלמיד עברו לריפו זה ונבדקו.
- אין שני מקורות אמת ואין העתקה ידנית קבועה בין ריפוזיטוריז.
- `main` הוא Production מוגן. בזמן ההגירה עובדים בענף מבודד ולא משנים את הקישור שכבר נשלח למורים.

## 2. מבנה יעד פשוט לעריכה

מבנה היעד בריפו:

```text
/
├─ SOURCE_OF_TRUTH.md        # מסמך הדרישות היחיד
├─ README.md                 # מצביע בלבד למסמך האמת + פקודות הפעלה קצרות
├─ src/                      # קוד המוצר
│  ├─ data/worksheetPages.tsx
│  ├─ components/worksheet/
│  ├─ styles/
│  └─ ...
├─ scripts/                  # build, manifest, verification
├─ tests/                    # בדיקות מבנה/מתמטיקה/regression
├─ public/                   # רק נכסים שבאמת נדרשים
├─ preview/                  # generated; לא מקור אמת
└─ .github/workflows/        # CI/Preview/Release
```

- אין מעטפת `sources/` ואין nesting מיותר.
- `src/data/worksheetPages.tsx` הוא אינדקס המימוש של הדפים; הוא **אינו** מסמך דרישות נוסף.
- לכל דף `key` יציב. המספר המוצג נגזר מהסדר ואינו מזהה קבוע.
- ככל שאפשר, שינוי רוחבי מתבצע דרך רכיב משותף/design token ולא עשרות תיקונים נקודתיים.

## 3. כלל ברזל — חוברת תלמיד בלבד

- החוברת מתחילה בעמוד **1**.
- 0 דפי פתיחה למורה ו־0 דפי מורה בחוברת המוצגת/מודפסת לתלמיד.
- אין כותרת `יחס · למורה`.
- אין עמודים לא ממוספרים לפני עמוד 1.
- כל דפי התלמיד מקבלים מספור רציף.
- נכון להגירה הנוכחית קיימים **55 דפי תלמיד סמנטיים**; המספר בעתיד נגזר מהמקור ולא נכתב כ־magic number בלוגיקת המוצר.

## 4. תוכן ומתמטיקה

- לא ממציאים שאלות, נתונים, תשובות או פתרונות שלא אושרו.
- שינוי תוכן מתמטי מחייב בדיקת מקור/אימות מתמטי.
- דיוק מתמטי קודם לעיצוב.
- RTL הוא ברירת המחדל; ביטויים מתמטיים, קואורדינטות ו־SVG מקבלים LTR נקודתי כשצריך.
- סימן כפל מוצג: `·`.
- סעיפים: `א.` `ב.` `ג.` ללא סוגריים.

## 5. עיצוב וגרפיקה — איכות מקסימלית

- המוצר הסופי הוא A4 להדפסה; מסך/מובייל הם Preview וניווט.
- לא מקטינים כתב ולא דוחסים שאלה כדי להכניס אותה לעמוד. אם אין מקום — מפצלים בצורה מבוקרת.
- שרטוטים, גרפים וצורות נוצרים כ־SVG/וקטור מדויק ככל האפשר, עם `viewBox`, מדידות מחושבות ו־`shape-rendering="geometricPrecision"` כאשר מתאים.
- אין rasterization של טקסט מתמטי או דף שלם כאשר אפשר לייצר DOM/SVG איכותי.
- צבע אקצנט קנוני: `#1f2a44` אלא אם יניב משנה במפורש.
- היררכיה, טיפוגרפיה, תיבות מענה, footer וכותרות אחידים בכל החוברת.
- גרפיקה חדשה צריכה להיות ניתנת להדפסה חדה, נגישה, וללא תלות ברזולוציית מסך.

## 6. מרחבי תשובה

- לכל סעיף שדורש כתיבה יש מקום מתאים לידו.
- יחס → `▭ : ▭`.
- זוג סדור → `( ▭ , ▭ )`.
- חישוב → אזור עבודה מתאים.
- הסבר/נימוק → אזור כתיבה רחב יותר.
- סימון/בחירה → ללא תיבה מיותרת.
- אין תיבת תשובה משותפת רחוקה בתחתית עמוד כאשר יש סעיפים נפרדים.
- אין תוויות גנריות `דרך`/`תשובה` כאשר צורת אזור המענה כבר ברורה.

## 7. טכנולוגיה — החזקה המתאימה ביותר

"הכי חזק" פירושו הטכנולוגיה היציבה והמתאימה ביותר לאיכות, דיוק ומהירות עריכה — לא שדרוג גרסה לשם שדרוג.

חובה:
- React + TypeScript במצב strict לממשק ורכיבים.
- Vite לבנייה מהירה ודטרמיניסטית.
- SVG וקטורי לגרפים/גאומטריה.
- Playwright + Chromium לבדיקות browser אמיתיות, screenshots ו־PDF.
- CSS Print / `@page` / A4 מדויק, עם `print-color-adjust`.
- פונטים מקומיים/מוטמעים; artifact סופי אינו תלוי באינטרנט לצורך layout.
- SHA-256/metadata לחיבור בין source commit לבין artifact שנבדק.
- CI שמפיל build על regression ולא "מתקן" אותו בשקט.

אסור:
- נתיבי מחשב מוחלטים.
- תלות ב־`razpages` לצורך build/test/preview.
- מספרי דפים קשיחים היסטוריים בבדיקות.
- שתי lockfiles פעילות לאותו package manager. ברירת המחדל היא `npm` + `package-lock.json` אחד.
- CSS override נקודתי חדש כשאפשר לפתור ברכיב/primitive משותף.

## 8. בדיקות חובה לפני Preview

כל Preview חייב לעבור לפחות:

- TypeScript typecheck.
- unit/structure tests.
- בדיקת keys ייחודיים ומספור רציף.
- 0 דפי מורה.
- build נקי בסביבה חדשה.
- manifest/hash.
- בדיקת Chromium של כל העמודים.
- A4 width/height.
- 0 overflow.
- 0 SVG מחוץ לעמוד.
- 0 שגיאות console/page.
- 0 בקשות רשת חיצוניות של artifact הסופי.
- screenshots של כל העמודים.
- PDF print-preview.
- בדיקת הדפסה גם לאחר פתיחה ב־viewport צר: `zoom`/responsive אינם רשאים לשנות את A4 המודפס.

## 9. Preview ו־Production

- Preview לעולם אינו מחליף Production אוטומטית.
- הקישור הציבורי שכבר נשלח למורים נשאר ללא שינוי בזמן פיתוח.
- יניב רואה Preview מלא לפני release.
- רק משפט אישור מפורש של יניב מאפשר שינוי של Production.
- לפני release שומרים SHA/נקודת rollback של Production הקודם.
- release חייב להיות traceable: source SHA, build id, page count ו־content hash.

## 10. קלות עריכה — יעד מוצר

המטרה היא שבקשה כמו "הגדל את מקום החישוב בכל החוברת" או "החלף סגנון כותרת" תהיה שינוי אחד או קטן ומבוקר, לא עשרות עריכות ידניות.

לכן:
- primitives משותפים ל־PageLayout, Question, SubQuestion, WorkArea, FinalAnswer, RatioAnswer, OrderedPairAnswer, Table, Figure ו־Footer.
- design tokens מרכזיים למרווחים, גדלים, typography, צבעים ועובי קווים.
- schema מפורש לסוגי תשובה במקום inference לפי מילים כאשר השאלה דורשת התנהגות מיוחדת.
- דפים חריגים מקבלים override מינימלי ומנומק בלבד.
- generated artifacts אינם נערכים ידנית.

## 11. סנכרון תמידי

- שינוי דרישה: `SOURCE_OF_TRUTH.md` קודם → קוד → tests → Preview.
- שינוי קוד שאינו דורש שינוי דרישה: קוד → tests → Preview; אין לשכפל כלל למסמך אחר.
- CI בודק שה־README מצביע למסמך הזה ואינו מכיל ספר כללים חלופי.
- CI בודק שאין import/path פעיל ל־`sources/lovable/ratio-workbook` או ל־`razpages` לאחר סיום ההגירה.
- artifact נוצר רק מה־commit שנבדק; אין העתקה ידנית של HTML ממקור אחר.

## 12. תנאי סגירת המקור הישן

אסור לסגור/למחוק/לארכב את המקור הישן לפני שכל התנאים הבאים ירוקים:

- [ ] כל 55 דפי התלמיד קיימים בריפו החדש.
- [ ] כל הרכיבים המשותפים עברו.
- [ ] כל CSS/פונטים/נכסים שנדרשים בפועל עברו.
- [ ] scripts של build/manifest/verify עברו והם עצמאיים.
- [ ] כל tests עברו והותאמו למזהים יציבים.
- [ ] Preview מלא נבנה מהריפו החדש בלבד.
- [ ] Chromium audit עבר על כל העמודים.
- [ ] PDF + screenshots נוצרו.
- [ ] אין reference פעיל ל־`razpages`/`sources` בקוד או ב־CI.
- [ ] יניב ראה את ה־Preview ואישר את מבנה הפרויקט.

רק לאחר שכל הסעיפים מסומנים ניתן להפסיק להשתמש ב־`sources/lovable/ratio-workbook`.

## 13. סטטוס הגירה — 2026-08-19

- Production הנוכחי ב־`main` נשאר ללא שינוי.
- ענף ההגירה: `agent/single-source-migration-20260819`.
- מקור ההגירה הזמני: `yanivmizrachiy/razpages`, branch `agent/ratio-preview-hardening-20260819`, path `sources/lovable/ratio-workbook/`.
- יעד: להעביר רק את מה שנדרש לחוברת היחס; לא להעתיק פסולת/קבצים היסטוריים לא־פעילים.
- לאחר ההעברה README יישאר מצביע קצר בלבד למסמך זה.
