name: Publish verified ratio artifact

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/publish-verified-artifact.yml'
  workflow_dispatch:

permissions:
  contents: write
  actions: read

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main

      - name: Download exact verified artifact 133
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p /tmp/ratio-artifact
          gh api repos/${GITHUB_REPOSITORY}/actions/artifacts/9443955670/zip > /tmp/ratio-artifact.zip
          unzip -q /tmp/ratio-artifact.zip -d /tmp/ratio-artifact
          test -f /tmp/ratio-artifact/preview/full-workbook.html
          echo '7cf8dcd3db8012edbf9df4c9778ed1d414bb929c9c2085ec0d5d1b4dd2be6534  /tmp/ratio-artifact/preview/full-workbook.html' | sha256sum -c -
          grep -q "page.setAttribute('data-front-page',String(i+1))" /tmp/ratio-artifact/preview/full-workbook.html
          grep -q 'wb-front-page' /tmp/ratio-artifact/preview/full-workbook.html

      - name: Publish only the verified workbook HTML
        run: |
          set -euo pipefail
          cp /tmp/ratio-artifact/preview/full-workbook.html index.html
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html
          if git diff --cached --quiet; then
            echo 'Production already matches verified artifact 133.'
            exit 0
          fi
          git commit -m 'publish: verified ratio workbook artifact 133 with color cover'
          git push origin HEAD:main
